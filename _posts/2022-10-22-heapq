# python 的优先队列 heapq

优先队列是一种数据结构，它类似于普通队列，但与普通队列不同的是，每个元素都有一个关联的优先级。在优先队列中，具有较高优先级的元素将首先被取出。

    import heapq

    # 创建一个空的优先队列
    queue = []

    # 向队列中插入元素
    heapq.heappush(queue, (2, 'A'))
    heapq.heappush(queue, (1, 'B'))
    heapq.heappush(queue, (3, 'C'))

    # 从队列中取出具有最高优先级的元素
    while queue:
        item = heapq.heappop(queue)
        print(item[1])

